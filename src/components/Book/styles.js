import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  bookView: {
    width: 100,
    height: 120,
    marginVertical: 15,
    marginRight: 15,
  },
  book: {
    position: 'absolute',
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
  },
});

export default styles;
