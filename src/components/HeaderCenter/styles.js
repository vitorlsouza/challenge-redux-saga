import { StyleSheet } from 'react-native';
import { colors } from '../../styles';

const styles = StyleSheet.create({
  input: {
    borderBottomWidth: 2,
    borderBottomColor: colors.blackTranparent,
    fontSize: 20,
    paddingVertical: 15,
  },
});

export default styles;
